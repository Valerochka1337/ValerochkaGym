# Yarumo coach

Android-приложение для ведения силовых тренировок: планы тренировок, библиотека упражнений с
разметкой мышц и отдельными карточками статистики, активная тренировка с таймером отдыха,
календарь тренировок с планированием, аналитика нагрузки и прогресса, синхронизация
с backend api.valerochkagym.tech и отдельная интеграция Google Calendar.

- Пакет: `com.valerochka1337.valerochkagym`
- minSdk 36, Kotlin + Jetpack Compose (Material 3), Hilt, Room, DataStore.

## Бренд и совместимость

Пользовательское название приложения — **Yarumo coach**. Ребрендинг не меняет техническую
идентичность уже установленного приложения: package/application ID и namespace остаются
`com.valerochka1337.valerochkagym`, тема — `Theme.ValerochkaGym`, а release-подпись и manifest
authorities `${applicationId}.inbody-import` и `${applicationId}.androidx-startup` сохраняются.
Локальная база остаётся `gym.db`; DataStore-файлы — `settings`, `ai_secrets` и
`weekly_schedule_operations`; стабильные UUID-префиксы `ValerochkaGym.exercise:`,
`ValerochkaGym.canonical-exercise:` и `ValerochkaGym.schedule:` также не переименовываются.

Google OAuth продолжает использовать существующий ключ ресурса `google_web_client_id` и прежнюю
Android-привязку package name + SHA-1. Репозиторий обновлений остаётся
`Valerochka1337/ValerochkaGym`, а ожидаемый APK asset — `ValerochkaGym-v<version>.apk`.
При необходимости владелец отдельно меняет отображаемое имя consent screen в Google Cloud Console:
это внешняя настройка, не поставляемая Android-репозиторием.

## Аккаунт и backend

Вход — через Google или по email и паролю. Кнопка Google доступна на экранах входа
и регистрации, без заполнения email и пароля. Регистрация по email и восстановление пароля используют восьмизначный
код из письма; серверу нужен настроенный SMTP. После успешной отправки приложение само
открывает ввод кода, после подтверждения возвращает ко входу. Пароль — 12–128 символов.

Настройки → Аккаунт: email, состояние сохранения тренировок, устройства и выход.
После выхода можно войти с другим email без очистки приложения. Вход в другой аккаунт
удаляет историю предыдущего пользователя с устройства вместе с очередью отправки;
сохранённая на сервере история остаётся в прежнем аккаунте. Повторный вход в тот же аккаунт
сохраняет его изменения без сети. Выход с устройства работает без интернета; выход со всех
устройств требует ответа сервера. Активную тренировку нужно сначала завершить.

Старые локальные данные без аккаунта не переносятся при первом входе. Приложение в бете;
экраны регистрации и настроек не предлагают экспорт базы, миграцию или сброс приложения.
Google Calendar и AI настраиваются отдельно в разделе «Подключения».

Room v15 хранит baseline и outbox; токены зашифрованы Keystore и не переносятся с backup.
Синхронизация запускается после изменений, при запуске и периодически при сети.
Во время активной тренировки она отложена до завершения. Sheets больше не используется.

## Порядок упражнений

В редакторе программы и в текущей тренировке порядок меняется перетаскиванием за grip справа от
названия упражнения. Grip — единственная зона начала переноса: поля, подходы и прокрутка списка
остаются независимыми от жеста. Во время переноса список автоматически прокручивается у края;
TalkBack предлагает для каждой карточки действия **«Переместить выше»** и **«Переместить ниже»**.

Это два разных порядка. В редакторе после **«Сохранить»** меняется порядок упражнений программы.
В уже начатой тренировке перестановка сохраняется только в текущей сессии и не переписывает
исходную программу. После завершения предложение **«Обновить программу?»** при подтверждении
сможет применить фактически выполненный порядок вместе с подходами.

## Календарь и расписание

Вкладка **Календарь** заменяет прежнюю «Историю» и совмещает историю (что сделано) и
планирование (что запланировано). Месячная сетка Пн–Вс листается стрелками и свайпом; в каждом дне:

- прошлые дни — **залитая точка**, если в этот день была завершённая тренировка;
- сегодня и будущее — **контурная точка**, если день запланирован.

Тап по дню открывает нижнюю шторку. Из неё можно открыть детали завершённой тренировки,
начать/удалить запланированную или, для пустого будущего дня, нажать **«Запланировать»**.

Есть два вида запланированных тренировок:

| Вид | Что это | Хранение | Google Calendar | Удаление |
|---|---|---|---|---|
| **Ad-hoc** | тап по дню → программа + время | `ScheduledWorkoutEntity` (Room) | одно событие | по одной |
| **Расписание** | недельный шаблон (7 дней) | `WeeklySchedule` в DataStore | одно RRULE-событие на день недели (`FREQ=WEEKLY`, бессрочно) | вся серия целиком |

Редактор расписания открывается иконкой в шапке календаря. **«Сохранить»** применяет шаблон с
заменой серии (старые события удаляются, создаются новые), **«Очистить расписание»** удаляет
всю серию и шаблон. Для планирования нужен выданный доступ к Google Calendar (см. настройку ниже);
повторяющиеся события создаются с явной таймзоной устройства — этого требует Google Calendar API.

## Тренажёрные залы

В **Настройки → Тренажёрные залы** можно создать несколько залов и отметить упражнения
(`ExerciseEntity`), доступные в каждом. Новая программа выбирает 0..N залов:

- без выбора доступен весь общий каталог;
- при одном зале доступен его состав;
- при нескольких — только пересечение, то есть упражнения, присутствующие во всех них.

Выбранные залы видны на карточке программы. Если программа содержит несовместимое упражнение,
сохранение и старт блокируются с явным списком; состав связанного зала также нельзя сузить молча,
а сам зал нельзя удалить до снятия связей. Активная тренировка наследует снимок залов программы.
Создание упражнения из такого picker сразу сохраняет его в общем каталоге, добавляет во все
выбранные залы и выбирает для программы либо добавляет в активную тренировку.

Room обеспечивает работу без интернета, backend хранит подтверждённые данные аккаунта.
UUID и связи сохраняются между установками. Очередь повторяет неподтверждённые запросы,
удаления синхронизируются, конфликтующие правки требуют явного выбора версии.

## Мышцы упражнений

Упражнение описывается не одной группой мышц, а **картой вовлечения**: жим лёжа — это грудь 100%,
трицепс 65%, передняя дельта 60% и пресс 10%. Крупная категория (`MuscleGroup`) осталась для
фильтров библиотеки и колонки `muscle_group` в выгрузке, а карта (`Muscle` × доля 0–100%) питает
всю аналитику.

| Что | Где |
|---|---|
| 18 мышц второго уровня | `data/db/entity/Muscle.kt` |
| Таблица `exercise_muscles` | `data/db/entity/ExerciseMuscleEntity.kt`, миграция `MIGRATION_2_3` |
| Карты встроенного каталога | `data/db/SeedExerciseMuscles.kt` (68 упражнений) |
| Фоллбэк по группе мышц | `data/db/MuscleDefaults.kt` |

Числа собраны по ЭМГ-исследованиям и анатомическим таблицам (Coratella, Saeterbakken, Youdas,
Martín-Fuentes, ExRx, обзоры Schoenfeld) и приведены к общей шкале: **100** — целевая мышца,
**55–85** — сильный синергист, **25–50** — заметное участие, **10–20** — стабилизатор.

Карту можно править из карточки упражнения кнопкой **«Редактировать»**. Редактор показывает
интерактивную модель тела: нажатие на мышцу добавляет её, ползунок задаёт долю 0–100%. Тот же
редактор создаёт своё упражнение (кнопка «+» в библиотеке), и тогда крупная группа выводится
автоматически из самой вовлечённой мышцы. У встроенных упражнений название не меняется — по нему
карта ищется в каталоге, а синхронизация с backend сохраняет стабильные UUID.

Упражнения без карты (созданные раньше, приехавшие импортом) получают типичную для своей группы
разметку при следующем открытии базы — досевом в `ExerciseMuscleSeeder`.

## Карточка упражнения

Карточка открывается по тапу на упражнение в активной тренировке, редакторе программы, деталях
прошлой тренировки, итогах, прогрессе и рекордах аналитики. В библиотеке основной тап сохраняет
смысл выбора, а карточка открывается отдельной кнопкой информации справа.

На экране показаны название, тип, крупная группа, карта вовлечения мышц и статистика только по
**завершённым** тренировкам:

| Тип | Прошлый раз | График | Рекорды |
|---|---|---|---|
| Силовое | все выполненные подходы `вес×повторы` | оценочный одноповторный максимум по тренировкам | лучший e1RM, максимальный вес, лучший объём тренировки |
| На время | длительность каждого подхода | суммарное время за тренировку | самый долгий подход и максимальное суммарное время |
| Кардио | скорость, наклон и длительность | расчётная дистанция за тренировку | дистанция, скорость и наклон |

Тап по точке графика выбирает тренировку и показывает её дату с опорным результатом. Если истории
ещё нет, карточка объясняет, что статистика появится после первой завершённой тренировки. Кнопка
редактирования в шапке открывает существующий `ExerciseEditorSheet`; после сохранения профиль и
карта мышц обновляются реактивно.

## ИИ-создание упражнений

Кнопка **«+»** в библиотеке сначала открывает шторку с описанием упражнения. Укажите снаряд,
положение и движение — например, «тяга гантели к поясу одной рукой в наклоне». Приложение отправит
описание и текущий каталог вместе с картами мышц на настроенный API, после чего откроет
обычный редактор упражнения с черновиком. Ничего не записывается в базу, пока не нажать
**«Создать»** в редакторе.

Если это уже тот же вариант упражнения (синонимы и другой порядок слов допустимы), открывается
существующий редактор. Отличающийся снаряд, угол, хват или техника создают новый вариант. Кнопка
**«Создать вручную»** остаётся доступной без ключа и сети.

### Настройка нейросети

1. Откройте **Настройки → Нейросеть** и сохраните OpenAI-совместимый Base URL.
2. Сохраните API key и выберите из загруженного каталога chat-модель, которая умеет принимать
   изображения: одна модель используется и для упражнений, и для фото InBody.

Каталог загружается с `/v1/models` именно с сохранённым ключом, поэтому показывает доступные ему
модели. API не сообщает в этом ответе возможности модели: поддержку изображений нужно учитывать
при выборе. Запрос отправляется на `/v1/chat/completions` в формате `json_object`; точная схема
включается в системную инструкцию, а ответ дополнительно строго проверяется приложением. Если
выбранная модель станет недоступна, ИИ-форма предложит открыть настройки и выбрать другую.

API key шифруется через Android Keystore; после сохранения поле показывает только маску вида
`sk-************1234`. Ключ не переносится с резервной копией. При HTTP ключ, описание упражнения
и фото InBody передаются без транспортного шифрования.

## Анализы

Вкладка **Анализы** отвечает на два вопроса: «чего не хватает в плане» и «работает ли план».
Переключатель периода (4 недели / 12 недель / год / всё) стоит один раз над всеми карточками —
все графики показывают один и тот же срез.

| Карточка | Что показывает | На чём основана |
|---|---|---|
| Итоги периода | тренировок, рабочих подходов, тоннаж, средняя длительность, серия недель, кардио в МЕТ-минутах | норма ВОЗ 500–1000 МЕТ-мин/нед, уравнения ACSM для дорожки |
| Карта нагрузки | одна крупная фигура с переворотом спереди/сзади, залитая по недельному объёму; тап — подробности | ступени = зоны MEV / MAV / MRV |
| Объём по мышцам | bullet-график: факт против ориентиров, отсортировано по дефициту | 10–20 подходов/нед (Schoenfeld 2017, Baz-Valle 2022, Pelland 2026) + landmarks Israetel/RP |
| Частота и пауза | дней с последней работы по каждой мышце, метки на 2/4/7 днях | ≥ 2 раза в неделю на мышцу (Schoenfeld/Krieger 2016) |
| Недельный объём | подходы или тоннаж по неделям + средняя за закрытые недели | — |
| Скачок нагрузки | подходы за 7 дней к средней неделе за 28 дней | ACWR: коридор 0.8–1.3, > 1.5 — риск (Gabbett) |
| Баланс объёма | жим/тяга, перёд/зад, верх/низ, бицепс бедра/квадрицепс на логарифмической шкале | цель 1:1 и коридор ±25%; H:Q по объёму не ниже 0.5 |
| Прогресс по упражнению | тренд оценки одноповторного максимума + вердикт (растёт / плато / снижается) и силовая кривая «вес × повторения» | формула Эпли до 12 повторений; порог заметности max(1.25 кг, 1%) |
| Рекорды | лучший расчётный максимум по каждому упражнению за всю историю | — |

Ключевые соглашения расчёта (все — в `domain/analysis/AnalyticsEngine.kt`):

- **рабочий подход** — выполненный подход силового упражнения или упражнения на время; разминка
  отсекается эвристикой «легче 60% от лучшего подхода этого упражнения в этой тренировке»;
- **эффективный подход мышцы** — ступенчатый вес по доле вовлечения (прямая работа 1.0, косвенная
  0.5, стабилизация 0): именно ступени, а не `доля / 100`, валидированы мета-анализом объёма,
  иначе стабилизаторы набирают несуществующий объём;
- недельные величины — **средние за неделю окна**, поэтому 4 недели и год сравнимы между собой и
  с ориентирами;
- в аналитику попадают только **завершённые** тренировки: объём идущей ещё меняется.

## Настройка Google-интеграции

Чтобы работали вход через Google и отдельное подключение Calendar, нужно создать проект
в Google Cloud и вставить в приложение свой OAuth Web client ID. Всё делается один раз.

### 1. Создать проект

1. Откройте [Google Cloud Console](https://console.cloud.google.com/).
2. Вверху выберите **Select a project → New Project**, задайте имя (например, `ValerochkaGym`)
   и создайте проект.

### 2. Включить нужные API

В меню **APIs & Services → Library** включите два API:

- **Google Calendar API**

Для каждого нажмите **Enable**.

### 3. Настроить OAuth consent screen

1. Перейдите в **APIs & Services → OAuth consent screen**.
2. Тип пользователей — **External**, нажмите **Create**.
3. Заполните обязательные поля (название приложения, email поддержки, email разработчика).
4. На шаге **Scopes** можно ничего не добавлять — приложение запрашивает доступ во время работы.
5. На шаге **Test users** добавьте свой Google-аккаунт (иначе вход будет заблокирован, пока
   приложение не прошло верификацию).

### 4. Создать OAuth client ID для Web application

1. Перейдите в **APIs & Services → Credentials → Create Credentials → OAuth client ID**.
2. Тип приложения — **Web application**, задайте имя (например, `ValerochkaGym Web`).
3. Нажмите **Create** и скопируйте **Client ID** (вида `1234567890-xxxx.apps.googleusercontent.com`).
4. Вставьте его в `app/src/main/res/values/strings.xml` в строку `google_web_client_id`
   (в репозитории уже лежит ID проекта владельца — для своего проекта замените на свой).

> Именно **Web** client ID передаётся в `serverClientId` библиотеки входа — это не опечатка,
> так работает Credential Manager / Google Identity.

### 5. Создать OAuth client ID для Android

1. Снова **Create Credentials → OAuth client ID**, тип приложения — **Android**.
2. **Package name**: `com.valerochka1337.valerochkagym`.
3. **SHA-1**: получите отпечаток командой из корня проекта:

   ```bash
   ./gradlew signingReport
   ```

   Скопируйте значение `SHA1` для нужного варианта (debug для отладочной сборки).
4. Нажмите **Create**. Отдельно в приложение этот ID вставлять не нужно — Google связывает
   Android-приложение с проектом по package name и SHA-1.

## Настройки приложения

Экран **Настройки** (шестерёнка в шапке любой вкладки):

| Карточка | Что настраивается |
|---|---|
| Аккаунт Yarumo coach | Google/email, синхронизация, конфликты, сессии, удаление аккаунта |
| Google Calendar | отдельный Google-аккаунт и OAuth-доступ к календарю |
| Тренажёрные залы | названия залов и доступные в каждом упражнения |
| Нейросеть | Base URL, зашифрованный API key и общая модель для упражнений и InBody |
| Таймер отдыха | отдых по умолчанию (±15 с), автостарт после подхода, звук и вибрация уведомления |
| Интерфейс | виброотклик (тактильная отдача на нажатия по всему приложению) |
| Акцент | цвет интерфейса и иконки лаунчера (4 варианта) |
| Приложение | текущая версия, проверка GitHub Releases, скачивание и запуск установки обновления |
| Аккаунт | email, устройства, выход и удаление аккаунта |
| О приложении | обновления и версия |

Экспортированный файл — обычная база SQLite; восстановление — замена файла `gym.db` в данных
приложения (или просмотр любым SQLite-инструментом). «Очистить данные» стирает историю,
программы и свои упражнения, но сохраняет настройки и встроенный каталог.

## Обновления из GitHub

Release-сборка при холодном запуске проверяет последний стабильный
[GitHub Release](https://github.com/Valerochka1337/ValerochkaGym/releases). Если версия новее,
диалог позволяет обновиться, выбрать **«Позже»** только для текущего запуска или
**«Не напоминать»** до появления следующей версии. Та же проверка и повтор установки доступны в
**Настройки → Приложение**.

Скачанный APK не передаётся Android вслепую: приложение проверяет размер и SHA-256 из GitHub,
свой package name, версию, монотонный `versionCode` и совпадение release-подписи. После этого
Android один раз попросит разрешить Yarumo coach устанавливать приложения из этого источника и
покажет собственный экран подтверждения установки. Автопроверка намеренно отключена в debug APK,
потому что debug- и release-подписи различаются; ручная кнопка остаётся доступной.

## Сборка и тесты

```bash
./gradlew :app:assembleDebug
./gradlew :app:testDebugUnitTest
./gradlew :app:assembleRelease   # R8 + сжатие ресурсов; подпись настраивается отдельно
```

Графики и карта тела нарисованы вручную в Compose Canvas, поэтому у них есть тест отрисовки
(`AnalysisRenderTest`, Robolectric + нативная графика). Он не сравнивает пиксели с эталоном, но
падает на ошибках рендера и складывает снимки в `app/build/reports/analysis-render/`, включая
`exercise-detail.png` для карточки упражнения, — их удобно посмотреть глазами после правок вида.

## Документация

- [docs/design-system.md](docs/design-system.md) — дизайн-система и правила, как делать UI дальше
  (единственный акцент, плоские поверхности, иконки, пружинная моторика `GymMotion`,
  хаптика `GymHaptics`, плавающий навбар).
- [ARCHITECTURE.md](ARCHITECTURE.md) — слои, потоки данных, фоновые механизмы и осознанные
  технические решения.
- [CHANGELOG.md](CHANGELOG.md) — история версий.
- [docs/superpowers/specs/](docs/superpowers/specs) — спецификации приложения (функциональность).
