# Проверка диагностики AI Plan

- AC-001 / T-001 — выполнено: SyncReady.Failure сохраняет причину; репозиторий различает локальную валидацию и проверку ответа; UI использует фиксированные сообщения и список разрешённых кодов.
- AC-002 / T-002 — адресные регрессии прошли: категории и приватность сообщений, форма при отказе, отмена без ошибки/навигации, устаревшая ошибка после смены аккаунта, строгая валидация ответа и запрет запроса при активной тренировке.
- AC-003 / T-003 — origin/main повторно проверен: cc7937d, версия 1.3.38 (46). Версия исправления 1.3.39 (47). Ветка fix/calendar-ai-diagnostics.

Проверки:
- `ANDROID_HOME=/Users/raul/Library/Android/sdk ./gradlew spotlessApply :app:testDebugUnitTest --tests '*CalendarAi*Test' --tests '*BackendSyncTest'` — PASS.
- Первые попытки потребовали доступа к Gradle-кэшу и явного ANDROID_HOME. В новых тестах исправлены предположение об идентичности восстановленного корутинами исключения и зависимость ViewModel-проверки от асинхронных Room-потоков.
- Итоговый `:app:testDebugUnitTest` — PASS: 1273 теста, 1272 прошли, 1 пропущен, 0 failures/errors.
- `:app:assembleDebug` — PASS. APK: `app/build/outputs/apk/debug/app-debug.apk`; metadata подтверждает 1.3.39 (47).

Самопроверка diff: условия валидации сохранены, проверки epoch/generation остаются, логи и зависимости не добавлены. Схема и протокол запроса не изменены. Неизвестные исключения и коды не раскрываются в UI. По прямому запросу работа выполнялась одним агентом.

Первопричина отказа на Pixel 9 неизвестна. Реальные AI-запросы не отправлялись, телефон и браузер не использовались. Исправление не опубликовано и не установлено на устройство.
