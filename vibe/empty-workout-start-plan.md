# План: запуск без программы из пустого каталога (#5)

Основание: [Gate R](empty-workout-start-brief.md). Небольшая изолированная UI-правка:
используется короткий путь без отдельного исследования и изменения архитектуры.

| Задача | Владелец / файлы | Приёмка и проверка |
|---|---|---|
| T-001 | Один исполнитель: `ui/workouts/WorkoutsScreen.kt` | В EmptyState основное «Начать без программы» вызывает существующий `viewModel.startEmpty`; создание программы вторично. Loading и непустой каталог не меняют смысл. AC-001–004 |
| T-002 | Тот же: тесты WorkoutsScreen и при необходимости WorkoutsViewModel | Проверить наличие/действие кнопки только при загруженном пустом каталоге, отсутствие её в loading, использование существующего startEmpty/single-flight. Не дублировать тесты DAO создания пустой тренировки. AC-001–004 |
| T-003 | Тот же: `app/build.gradle.kts`, tracker | Один следующий patch/code bump относительно интегрированных этапов; targeted tests и форматирование |
| T-004 | Независимые tester/reviewer, затем root | Аудит AC и UI semantics без скриншотов; полный unit и debug после стабильного diff, отдельный commit/ветка |

Сначала принять #47, затем создать отдельную ветку `fix/empty-workout-start` от интеграции.
UI: существующие PillButton/MaterialTheme/GymMotion, строки Kotlin, понятные подписи и 48dp.
Не добавлять фиктивные программы/подходы или новую цепочку разрешений/запуска сервиса.
Правила восстановления разрешений придут отдельным #10 и сохранят это действие.
Откат — revert UI-коммита с новым монотонным номером выпущенной версии; схема не меняется.

Gate P: каждый AC Gate R покрыт T-001/T-002; один владелец, нет новых контрактов/миграций.
