<resources>
    <!-- Compose drives the in-app theme; the launch window follows the system appearance. -->
    <style name="Theme.ValerochkaGym" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">?android:attr/colorBackground</item>
        <item name="android:fontFamily">sans</item>
    </style>
</resources>
